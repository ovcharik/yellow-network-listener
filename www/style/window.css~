.window {
    position: absolute;
    left: 50%;
    top: 20px;
    width: 1px;
    z-index: 2;
}

.window .content {
    position: relative;
    display: block;
    
    padding: 10px;
    
    border-radius: 3px;
    background: rgba(255, 255, 255, 0.8);
    box-shadow: 0 0 10px rgba(0,0,0,0.5);
}

.window .overflow {
    position: relative;
    overflow: auto;
}

.window .title {
    text-shadow: 0 0 3px rgba(0, 0, 0, 0.3);
    font-size: 24px;
    text-align: left;
    border-bottom: 1px black solid;
}

.window .title2 {
    padding: 20px 0px 3px 0px;
    font-size: 18px;
    text-align: left;
}

.window .close-buttom {
    position: absolute;
    right: 5px;
    top: 5px;
    text-shadow: 0 0 3px rgba(0, 0, 0, 0.7);
    font-size: 20px;
    color: #FFF;
    font-weight: bolder;
    padding: 2px 10px;
    cursor: pointer;
}

.window .close-buttom:hover {
    border-radius: 5px;
    box-shadow: 0 0 2px rgba(0, 0, 0, 0.5);
    background: rgba(0, 0, 0, 0.1);
}

.window .close-buttom:active {
    border-radius: 5px;
    box-shadow: inset 0 0 2px rgba(0, 0, 0, 0.5);
    background: rgba(0, 0, 0, 0.1);
}

.window .back-buttom {
    position: relative;
    top: -5px;
    left: -3px;
    display: inline-block;
    text-shadow: 0 0 3px rgba(0, 0, 0, 0.7);
    font-size: 20px;
    color: #FFF;
    font-weight: bolder;
    padding: 2px 10px;
    margin-right: 5px;
    cursor: pointer;
}

.window .back-buttom:hover {
    border-radius: 5px;
    box-shadow: 0 0 2px rgba(0, 0, 0, 0.5);
    background: rgba(0, 0, 0, 0.1);
}

.window .back-buttom:active {
    border-radius: 5px;
    box-shadow: inset 0 0 2px rgba(0, 0, 0, 0.5);
    background: rgba(0, 0, 0, 0.1);
}

.window .label {
    font-size: 14px;
    padding: 15px 0px 3px 0px;
}

.window .input {
    border-radius: 2px;
    border: 1px black solid;
    padding: 2px;
    background: none;
    font-size: 12px;
    width: 148px;
    color: black;
}

.window .buttom {
    padding: 2px;
}

.window .info {
    font-size: 14px;
    padding-top: 10px;
}

.window .info .key {
    width: 30%;
    display: inline-block;
}

.window .info .value {
    width: 70%;
    display: inline-block;
}
